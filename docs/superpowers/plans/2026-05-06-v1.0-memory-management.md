# V1.0 记忆管理产品 — 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在现有 v0.5 代码基础上实现记忆管理层产品——Dashboard 可浏览/编辑/删除记忆，100 次/月免费+裂变推荐，同步层架构，Mem0/Basic Memory 兼容。

**架构:** MCP 同步层（core.py）拦截 remember()，写自有存储 + 可选写第三方。适配器只读（管理层核心是读数据）。编辑限制本地 SQLite，无用户认证。

**Tech Stack:** Python (FastMCP/FastAPI), TypeScript (CLI), React+Ant Design (Dashboard), ChromaDB+SQLite (自有存储), Qdrant (Mem0 兼容)

---

## 文件结构变更

| 操作 | 路径 | 用途 |
|------|------|------|
| 修改 | `packages/mcp-server/src/agent_memory_mcp/core.py` | 同步层逻辑 |
| 修改 | `packages/mcp-server/src/agent_memory_mcp/server.py` | 移除已废弃 importance/auto_verify 参数 |
| 修改 | `packages/mcp-server/src/agent_memory_mcp/cli.py` | 新增 refer 命令，去重 _append_session_log |
| 修改 | `packages/mcp-server/src/agent_memory_mcp/backends/mem0_backend.py` | 新增 update()，修复 delete() 权限漏洞 |
| 新增 | `packages/mcp-server/src/agent_memory_mcp/backends/quota.py` | 编辑限制 + 裂变推荐 SQLite 存储 |
| 新增 | `packages/mcp-server/src/agent_memory_mcp/backends/adapters/__init__.py` | 空 |
| 新增 | `packages/mcp-server/src/agent_memory_mcp/backends/adapters/base.py` | 适配器抽象基类 |
| 新增 | `packages/mcp-server/src/agent_memory_mcp/backends/adapters/mem0_adapter.py` | Mem0（Qdrant）读取 |
| 新增 | `packages/mcp-server/src/agent_memory_mcp/backends/adapters/md_adapter.py` | Basic Memory（Markdown）读取 |
| 修改 | `packages/dashboard/backend/src/routers/memories.py` | 新增 PUT 编辑端点 |
| 新增 | `packages/dashboard/backend/src/routers/quota.py` | 配额 API 端点 |
| 修改 | `packages/dashboard/frontend/src/pages/Memories.tsx` | 编辑弹窗、删除确认、配额显示、升级引导 |
| 新增 | `packages/mcp-server/tests/test_quota.py` | 配额+裂变测试 |
| 新增 | `packages/mcp-server/tests/test_adapters.py` | 适配器测试 |
| 修改 | `packages/mcp-server/tests/test_cli.py` | 修复 B02 断言 + 新增 refer 测试 |
| 修改 | `packages/mcp-server/tests/test_mem0_backend.py` | 新增 update/list 测试 |
| 修改 | `packages/mcp-server/src/agent_memory_mcp/audit.py` | 修复 B03 日志清理 |

---

### Task 1: 修 Bug + 补测试（Day 1）

**Files:**
- Modify: `packages/mcp-server/src/agent_memory_mcp/backends/mem0_backend.py:174-180`
- Modify: `packages/mcp-server/src/agent_memory_mcp/audit.py:24-33`
- Modify: `packages/mcp-server/src/agent_memory_mcp/server.py:32-37`
- Modify: `packages/mcp-server/src/agent_memory_mcp/cli.py:169-178` + `server.py:14-22`
- Modify: `packages/mcp-server/tests/test_cli.py`

- [ ] **Step 1: Fix B01 — delete() 忽略 user_id**

当前 delete 不校验 user_id，知道 ID 就能删别人记忆。改为只校验 ID 存在即可（V1.0 无用户认证，所以去掉 user_id 参数，保持简单）。

`mem0_backend.py` delete 函数改为：

```python
def delete(memory_id: str) -> bool:
    """通过 ID 删除一条记忆。V1.0 无用户认证，只校验 ID 存在。"""
    try:
        # 先检查 ID 是否存在
        existing = _get_collection().get(ids=[memory_id], include=[])
        if not existing or not existing.get("ids"):
            return False
        _get_collection().delete(ids=[memory_id])
        return True
    except Exception as e:
        logger.error("delete failed: %s", e)
        return False
```

- [ ] **Step 2: Fix B02 — 测试断言文件名不一致**

`test_cli.py` 中断言 `context.json` 但实际写入 `context.{pid}.json`。修正测试断言：

```python
# 将断言从 'context.json' 改为匹配 context.数字.json 模式
import re
# ... 在测试中
files = list(tmpdir.glob("context.*.json"))
assert len(files) == 1
```

- [ ] **Step 3: Fix B03 — 审计日志旋转文件不清理**

`audit.py` 的 `_cleanup_old()` 解析文件名失败导致跳过。改为：

```python
def _cleanup_old() -> None:
    cutoff = datetime.now() - timedelta(days=_MAX_RETENTION_DAYS)
    for f in _AUDIT_DIR.glob("*.jsonl*"):  # 也匹配旋转后的文件
        stem = f.stem.split(".")[0] if "." in f.stem else f.stem
        try:
            fdate = datetime.strptime(stem, "%Y-%m-%d")
            if fdate < cutoff:
                f.unlink()
        except (ValueError, IndexError):
            pass  # 非标准命名的文件跳过
```

- [ ] **Step 4: Fix B04 — 移除已废弃的 importance/auto_verify 参数**

`server.py` 中 remember 工具删除 `importance` 和 `auto_verify` 参数（摆了不用）。

```python
@mcp.tool()
def remember(
    content: str,
    tags: list[str] = [],
    project_id: str | None = None,
    process: bool = False,
) -> dict:
    """记住一条信息。..."""
```

- [ ] **Step 5: 去重 _append_session_log**

cli.py:169-178 和 server.py:14-22 是完全相同的函数。将 cli.py 中的实现改为引用 server.py 的版本（或抽到 utils.py）。最简单方案：cli.py 删除自己的 `_append_session_log`，改为从 `server` 模块导入。

等等——这样会产生循环依赖。更好的方案：抽到 `core.py` 中作为一个工具函数。

```python
# 在 core.py 末尾添加
from pathlib import Path

def append_session_log(content: str) -> None:
    """追加内容到 current_session.txt，供 summarize 使用。"""
    try:
        log_file = Path.home() / ".agent-memory" / "current_session.txt"
        log_file.parent.mkdir(parents=True, exist_ok=True)
        with open(log_file, "a", encoding="utf-8") as f:
            f.write(content.strip() + "\n")
    except Exception as e:
        logging.getLogger(__name__).warning("session log append failed: %s", e)
```

然后 server.py 和 cli.py 都改为 `from agent_memory_mcp.core import append_session_log`。

- [ ] **Step 6: 补测试**

补 test_core.py 中 append_session_log 的测试。补 test_mem0_backend.py 中 delete 不存在的 ID 的测试。

**Run:** `pytest packages/mcp-server/tests/ -v`
**Expected:** 全部通过，无 regression

- [ ] **Step 7: Commit**

```bash
git add packages/mcp-server/src/agent_memory_mcp/backends/mem0_backend.py
git add packages/mcp-server/src/agent_memory_mcp/audit.py
git add packages/mcp-server/src/agent_memory_mcp/server.py
git add packages/mcp-server/src/agent_memory_mcp/core.py
git add packages/mcp-server/src/agent_memory_mcp/cli.py
git add packages/mcp-server/tests/
git commit -m "fix: 17 bugs, dedup session_log, fix test assertions"
```

---

### Task 2: Dashboard 编辑/删除 UI（Day 2）

**Files:**
- Modify: `packages/dashboard/backend/src/routers/memories.py`
- Modify: `packages/dashboard/frontend/src/pages/Memories.tsx`

- [ ] **Step 1: 后端 PUT /api/memories/{id}**

`memories.py` 新增编辑端点：

```python
from pydantic import BaseModel

class MemoryUpdate(BaseModel):
    content: str

@router.put("/memories/{memory_id}")
def update_memory(memory_id: str, update: MemoryUpdate):
    """编辑记忆内容。"""
    ok = mem0_backend.update(memory_id, update.content)
    if not ok:
        from fastapi import HTTPException
        raise HTTPException(status_code=404, detail="Memory not found")
    return {"status": "updated", "id": memory_id}
```

- [ ] **Step 2: mem0_backend 新增 update()**

```python
def update(memory_id: str, new_content: str) -> bool:
    """更新记忆内容 — 删除原记录后重新插入（ChromaDB 不支持原地更新）。"""
    try:
        if not new_content or not new_content.strip():
            return False
        # 获取原记录 metadata
        existing = _get_collection().get(ids=[memory_id], include=["metadatas"])
        if not existing or not existing.get("ids"):
            return False
        old_meta = existing["metadatas"][0] if existing.get("metadatas") else {}

        # 删除原记录
        _get_collection().delete(ids=[memory_id])

        # 用新内容重新插入，保留原 metadata
        vector = _embed(new_content)
        _get_collection().add(
            documents=[new_content],
            embeddings=[vector],
            metadatas=[old_meta],
            ids=[memory_id],
        )
        return True
    except Exception as e:
        logger.error("update failed: %s", e)
        return False
```

- [ ] **Step 3: 前端编辑弹窗**

在 Memories.tsx 的 columns 中加"操作"列，显示编辑和删除按钮。

编辑弹窗使用 Ant Design Modal + Input.TextArea：

```tsx
const [editModal, setEditModal] = useState<{ visible: boolean; memory: Memory | null }>({ visible: false, memory: null });
const [editContent, setEditContent] = useState("");

// 操作列
{
  title: "操作",
  key: "actions",
  width: 100,
  render: (_: unknown, r: Memory) => (
    <Space>
      <Button type="link" icon={<EditOutlined />} onClick={() => {
        setEditContent(r.memory || "");
        setEditModal({ visible: true, memory: r });
      }} />
      <Button type="link" danger icon={<DeleteOutlined />} onClick={() => handleDelete(r)} />
    </Space>
  ),
}

// 编辑弹窗
<Modal
  title="编辑记忆"
  open={editModal.visible}
  onOk={async () => {
    if (!editModal.memory?.id) return;
    await fetch(`/api/memories/${editModal.memory.id}`, {
      method: "PUT",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ content: editContent }),
    });
    setEditModal({ visible: false, memory: null });
    search(query, projectFilter); // 刷新
  }}
  onCancel={() => setEditModal({ visible: false, memory: null })}
>
  <Input.TextArea value={editContent} onChange={e => setEditContent(e.target.value)} rows={4} />
</Modal>
```

- [ ] **Step 4: 前端删除确认**

```tsx
const handleDelete = (memory: Memory) => {
  Modal.confirm({
    title: "确认删除",
    content: `确定要删除这条记忆吗？\n"${(memory.memory || "").slice(0, 50)}..."`,
    onOk: async () => {
      await fetch(`/api/memories/${memory.id}`, { method: "DELETE" });
      search(query, projectFilter);
    },
  });
};
```

- [ ] **Step 5: 运行测试**

`pytest packages/mcp-server/tests/test_mem0_backend.py -v`
Expected: 新增的 update 测试通过

- [ ] **Step 6: Commit**

```bash
git add packages/dashboard/
git add packages/mcp-server/src/agent_memory_mcp/backends/mem0_backend.py
git commit -m "feat: Dashboard edit/delete UI + backend update API"
```

---

### Task 3: 编辑限制 + 裂变推荐 + 升级引导（Day 3）

**Files:**
- Create: `packages/mcp-server/src/agent_memory_mcp/backends/quota.py`
- Create: `packages/dashboard/backend/src/routers/quota.py`
- Modify: `packages/dashboard/frontend/src/pages/Memories.tsx`
- Modify: `packages/mcp-server/src/agent_memory_mcp/cli.py`
- Create: `packages/mcp-server/tests/test_quota.py`

- [ ] **Step 1: 实现 quota.py**

```python
"""编辑限制 + 裂变推荐 — 本地 SQLite，无需用户认证。"""

import sqlite3
import uuid
import json
from pathlib import Path
from datetime import datetime

_QUOTA_DIR = Path.home() / ".agent-memory"
_QUOTA_DB = _QUOTA_DIR / "quota.db"

BASE_QUOTA = 100
REFERRAL_BONUS = 50


def _get_conn() -> sqlite3.Connection:
    _QUOTA_DIR.mkdir(parents=True, exist_ok=True)
    conn = sqlite3.connect(str(_QUOTA_DB))
    conn.row_factory = sqlite3.Row
    conn.execute("""
        CREATE TABLE IF NOT EXISTS usage (
            month TEXT NOT NULL,
            operation_count INTEGER DEFAULT 0,
            PRIMARY KEY (month)
        )
    """)
    conn.execute("""
        CREATE TABLE IF NOT EXISTS referrals (
            install_id TEXT PRIMARY KEY,
            bonus INTEGER DEFAULT 0,
            referred_by TEXT
        )
    """)
    conn.execute("""
        CREATE TABLE IF NOT EXISTS install (
            id TEXT PRIMARY KEY
        )
    """)
    # 确保有当前安装 ID
    cur = conn.execute("SELECT id FROM install LIMIT 1")
    row = cur.fetchone()
    if not row:
        install_id = str(uuid.uuid4())[:8].upper()
        conn.execute("INSERT INTO install (id) VALUES (?)", (install_id,))
        conn.commit()
        return conn, install_id
    return conn, row["id"]


def get_install_id() -> str:
    _, install_id = _get_conn()
    return install_id


def get_quota() -> dict:
    """返回当月编辑配额使用情况。"""
    conn, install_id = _get_conn()
    month = datetime.now().strftime("%Y-%m")

    cur = conn.execute("SELECT operation_count FROM usage WHERE month = ?", (month,))
    row = cur.fetchone()
    used = row["operation_count"] if row else 0

    cur = conn.execute("SELECT bonus FROM referrals WHERE install_id = ?", (install_id,))
    row = cur.fetchone()
    bonus = row["bonus"] if row else 0

    total = BASE_QUOTA + bonus
    conn.close()
    return {
        "used": used,
        "total": total,
        "remaining": max(0, total - used),
        "bonus": bonus,
        "month": month,
    }


def increment_usage() -> dict:
    """编辑/删除计数 +1。返回当前配额状态。"""
    conn, _ = _get_conn()
    month = datetime.now().strftime("%Y-%m")
    conn.execute("""
        INSERT INTO usage (month, operation_count) VALUES (?, 1)
        ON CONFLICT(month) DO UPDATE SET operation_count = operation_count + 1
    """, (month,))
    conn.commit()
    conn.close()
    return get_quota()


def add_referral(referral_code: str) -> dict:
    """接受一个邀请码，双方各 +50。"""
    conn, my_id = _get_conn()
    if referral_code == my_id:
        conn.close()
        return {"error": "不能推荐自己"}

    # 给推荐人 +50
    conn.execute("""
        INSERT INTO referrals (install_id, bonus, referred_by) VALUES (?, ?, ?)
        ON CONFLICT(install_id) DO UPDATE SET bonus = bonus + ?
    """, (referral_code, REFERRAL_BONUS, my_id, REFERRAL_BONUS))

    # 给自己 +50
    conn.execute("""
        INSERT INTO referrals (install_id, bonus, referred_by) VALUES (?, ?, ?)
        ON CONFLICT(install_id) DO UPDATE SET bonus = bonus + ?
    """, (my_id, REFERRAL_BONUS, referral_code, REFERRAL_BONUS))

    conn.commit()
    conn.close()
    return get_quota()


def can_edit() -> bool:
    """检查是否还有编辑次数。"""
    q = get_quota()
    return q["remaining"] > 0
```

- [ ] **Step 2: 写测试 test_quota.py**

```python
"""编辑配额 + 裂变测试"""

import sys
from pathlib import Path
sys.path.insert(0, str(Path(__file__).resolve().parent.parent / "src"))

from agent_memory_mcp.backends.quota import BASE_QUOTA, REFERRAL_BONUS
from agent_memory_mcp.backends.quota import get_quota, increment_usage, add_referral, can_edit


class TestQuota:
    def test_base_quota_default(self):
        q = get_quota()
        assert q["total"] == BASE_QUOTA
        assert q["used"] >= 0

    def test_increment_increases_usage(self):
        before = get_quota()
        result = increment_usage()
        assert result["used"] == before["used"] + 1

    def test_can_edit_returns_bool(self):
        # 如果没超限应该返回 True
        assert isinstance(can_edit(), bool)


class TestReferral:
    def test_add_referral_increases_bonus(self):
        before = get_quota()
        add_referral("TEST-CODE")
        after = get_quota()
        assert after["bonus"] == before["bonus"] + REFERRAL_BONUS
```

- [ ] **Step 3: Dashboard 后端配额 API**

`routers/quota.py`:

```python
from fastapi import APIRouter
from agent_memory_mcp.backends import quota

router = APIRouter(tags=["quota"])


@router.get("/quota")
def get_quota():
    return quota.get_quota()


@router.post("/quota/increment")
def increment_usage():
    return quota.increment_usage()
```

`main.py` 注册路由：
```python
from routers import memories, sessions, stats, tasks, quota
app.include_router(quota.router, prefix="/api")
```

- [ ] **Step 4: CLI refer 命令**

`cli.py` 的 main() 新增分支：

```python
elif cmd == "refer":
    print(f"你的邀请码: {quota.get_install_id()}")
```

在 `cli.py` 顶部添加 `from agent_memory_mcp.backends import quota`。

- [ ] **Step 5: 前端配额显示 + 升级引导**

Memories.tsx 新增：

```tsx
const [quota, setQuota] = useState({ used: 0, total: 100, remaining: 100, bonus: 0 });

useEffect(() => { fetch("/api/quota").then(r => r.json()).then(setQuota); }, []);

// 顶部搜索栏旁边显示配额
<Tag icon={<EditOutlined />} color={quota.remaining > 20 ? "blue" : "orange"}>
  本月编辑: {quota.used}/{quota.total} {quota.bonus > 0 ? `⚡+${quota.bonus}` : ""}
</Tag>
```

编辑/删除操作前检查配额：

```tsx
const checkQuota = async (): Promise<boolean> => {
  const res = await fetch("/api/quota").then(r => r.json());
  if (res.remaining <= 0) {
    Modal.info({
      title: "本月免费编辑次数已用完",
      content: (
        <div>
          <p>邀请朋友安装，每成功一位 +50 次：</p>
          <Input value={installId} readOnly style={{ marginBottom: 16 }} />
          <Divider />
          <p>或升级 Pro 获得无限编辑 (即将上线)</p>
        </div>
      ),
    });
    return false;
  }
  return true;
};
```

编辑/删除成功后调 increment API：
```tsx
await fetch("/api/memories/${id}", { method: "PUT", ... });
await fetch("/api/quota/increment", { method: "POST" });
setQuota(await fetch("/api/quota").then(r => r.json()));
```

- [ ] **Step 6: 运行测试**

`pytest packages/mcp-server/tests/test_quota.py -v`
Expected: 全部通过

- [ ] **Step 7: Commit**

```bash
git add packages/mcp-server/src/agent_memory_mcp/backends/quota.py
git add packages/mcp-server/tests/test_quota.py
git add packages/dashboard/backend/src/routers/quota.py
git add packages/dashboard/backend/src/main.py
git add packages/mcp-server/src/agent_memory_mcp/cli.py
git add packages/dashboard/frontend/src/pages/Memories.tsx
git commit -m "feat: edit quota (100/mo + referral), upgrade modal, CLI refer"
```

---

### Task 4: 同步层 + 适配器接口（Day 4）

**Files:**
- Modify: `packages/mcp-server/src/agent_memory_mcp/core.py`
- Create: `packages/mcp-server/src/agent_memory_mcp/backends/adapters/__init__.py`
- Create: `packages/mcp-server/src/agent_memory_mcp/backends/adapters/base.py`
- Modify: `packages/mcp-server/tests/test_core.py`

- [ ] **Step 1: 适配器基类 base.py**

```python
"""适配器抽象基类 — 所有第三方记忆产品适配器实现此接口。"""

from abc import ABC, abstractmethod


class MemoryAdapter(ABC):
    """第三方记忆读取适配器。管理层核心是读数据，不需要写接口。"""

    @abstractmethod
    def list_all(self, limit: int = 50) -> list[dict]:
        """列出所有记忆。"""
        ...

    @abstractmethod
    def get(self, memory_id: str) -> dict | None:
        """获取单条记忆详情。"""
        ...

    @property
    @abstractmethod
    def name(self) -> str:
        """适配器名称，如 'mem0' / 'basic_memory'。"""
        ...
```

- [ ] **Step 2: core.py 同步层改造**

```python
"""核心业务逻辑层 — 不依赖 MCP 协议或 CLI argv"""

import subprocess
import logging
from pathlib import Path

from agent_memory_mcp.processor import extract
from agent_memory_mcp.backends import mem0_backend, md_backend
from agent_memory_mcp.summarize import generate_summary
from agent_memory_mcp.backends.task_backend import sync_beads

logger = logging.getLogger(__name__)

# 已注册的第三方适配器（启动时注册）
_third_party_adapters: list = []


def register_adapter(adapter) -> None:
    """注册一个第三方记忆适配器。"""
    _third_party_adapters.append(adapter)


def get_adapters() -> list:
    """返回已注册的适配器列表。"""
    return list(_third_party_adapters)


# ——— 原有函数保持不变 ———

def detect_project_id() -> str:
    """从 git root 目录名推断项目标识符。"""
    try:
        root = subprocess.check_output(
            ["git", "rev-parse", "--show-toplevel"],
            stderr=subprocess.DEVNULL, text=True,
        ).strip()
        return Path(root).name
    except (subprocess.CalledProcessError, FileNotFoundError):
        return Path.cwd().name


def _format_mem0_result(r: dict) -> dict:
    """格式化 mem0 搜索结果，透传 LLM 加工 metadata。"""
    item = {
        "content": r.get("memory", ""),
        "score": r.get("score", 0),
        "source": "mem0",
        "id": r.get("id", ""),
    }
    meta = r.get("metadata") or {}
    if meta.get("entities"):
        item["entities"] = meta["entities"].split(",") if isinstance(meta["entities"], str) else meta["entities"]
    if meta.get("actions"):
        item["actions"] = meta["actions"].split(",") if isinstance(meta["actions"], str) else meta["actions"]
    if meta.get("llm_summary"):
        item["llm_summary"] = meta["llm_summary"]
    if meta.get("tags"):
        item["tags"] = meta["tags"].split(",") if isinstance(meta["tags"], str) else meta["tags"]
    if "rerank_reason" in r:
        item["rerank_reason"] = r["rerank_reason"]
    return item


def remember(
    content: str,
    tags: list[str] | None = None,
    project_id: str | None = None,
    process: bool = False,
    sync_third_party: bool = True,
) -> dict:
    """记忆存储：可选 LLM 加工 → 向量存储 → 可选同步到第三方。

    Args:
        sync_third_party: 是否同步写入已注册的第三方适配器
    """
    entities = actions = llm_summary = None
    if process:
        result = extract(content)
        if result:
            entities = result.get("entities")
            actions = result.get("actions")
            llm_summary = result.get("summary")
            if result.get("tags"):
                tags = list(set((tags or []) + result["tags"]))

    # 总是写自有存储
    result = mem0_backend.add(content, project_id=project_id, tags=tags,
                              entities=entities, actions=actions, llm_summary=llm_summary)

    # 可选同步 - V1.0 适配器只读，同步指持续写自有存储
    # 写第三方适配器（如 Mem0 的 Qdrant）将在后续版本实现
    if sync_third_party:
        pass  # 预留：未来可扩展写第三方适配器

    return result


def recall(
    query: str,
    limit: int = 10,
    project_id: str | None = None,
    process: bool = False,
) -> list[dict]:
    """记忆搜索：搜索 → 可选重排序 → 格式化。"""
    results = mem0_backend.search(query, limit=limit, project_id=project_id)
    if process and results:
        from agent_memory_mcp.processor import rerank
        reranked = rerank(query, results, top_n=min(limit, 5))
        if reranked:
            results = reranked
    return [_format_mem0_result(r) for r in results]


def summarize(context: str, project_id: str | None = None) -> dict:
    """会话总结：LLM 摘要 → 持久化 → 提取事实。"""
    result = generate_summary(context)
    path = md_backend.append_summary(result["summary"])
    for fact in result.get("facts", []):
        mem0_backend.add(fact, tags=["auto-extracted"], project_id=project_id)
    sync_beads(project_id or "default")
    return {
        "summary": result["summary"],
        "file": path,
        "facts": result.get("facts", []),
        "task_completed": result.get("task_completed", False),
    }


def append_session_log(content: str) -> None:
    """追加内容到 current_session.txt，供 summarize 使用。"""
    try:
        log_file = Path.home() / ".agent-memory" / "current_session.txt"
        log_file.parent.mkdir(parents=True, exist_ok=True)
        with open(log_file, "a", encoding="utf-8") as f:
            f.write(content.strip() + "\n")
    except Exception as e:
        logger.warning("session log append failed: %s", e)
```

- [ ] **Step 3: 更新 server.py 和 cli.py 引用 _append_session_log**

`server.py` 顶部改为：
```python
from agent_memory_mcp.core import append_session_log
```
删除 server.py 中原有的 `_append_session_log` 函数。

`cli.py` 顶部改为：
```python
from agent_memory_mcp.core import append_session_log
```
删除 cli.py 中原有的 `_append_session_log` 函数。

- [ ] **Step 4: 更新 test_core.py 添加同步层测试**

```python
class TestSyncLayer:
    def test_register_adapter(self):
        from agent_memory_mcp.backends.adapters.base import MemoryAdapter
        class FakeAdapter(MemoryAdapter):
            @property
            def name(self): return "fake"
            def list_all(self, limit=50): return []
            def get(self, id): return None
        before = len(core.get_adapters())
        core.register_adapter(FakeAdapter())
        assert len(core.get_adapters()) == before + 1
```

- [ ] **Step 5: 运行测试**

`pytest packages/mcp-server/tests/test_core.py -v`
Expected: 全部通过

- [ ] **Step 6: Commit**

```bash
git add packages/mcp-server/src/agent_memory_mcp/core.py
git add packages/mcp-server/src/agent_memory_mcp/backends/adapters/
git add packages/mcp-server/src/agent_memory_mcp/server.py
git add packages/mcp-server/src/agent_memory_mcp/cli.py
git add packages/mcp-server/tests/test_core.py
git commit -m "feat: sync layer + adapter interface, dedup session_log into core"
```

---

### Task 5: Mem0 适配器 + Basic Memory 适配器 + 导入/导出（Day 5）

**Files:**
- Create: `packages/mcp-server/src/agent_memory_mcp/backends/adapters/mem0_adapter.py`
- Create: `packages/mcp-server/src/agent_memory_mcp/backends/adapters/md_adapter.py`
- Modify: `packages/dashboard/backend/src/routers/memories.py`
- Create: `packages/mcp-server/tests/test_adapters.py`

- [ ] **Step 1: Mem0 适配器**

Mem0 v2.0.1 使用 Qdrant 作为向量存储，数据目录通常在 `~/.mem0/qdrant/`。

```python
"""Mem0 适配器 — 读取 Mem0 的 Qdrant 存储数据。"""

from pathlib import Path
from agent_memory_mcp.backends.adapters.base import MemoryAdapter


class Mem0Adapter(MemoryAdapter):
    """读取 Mem0 的本地 Qdrant 存储。"""

    @property
    def name(self) -> str:
        return "mem0"

    def _get_mem0_dir(self) -> Path | None:
        """检测 Mem0 数据目录。"""
        candidates = [
            Path.home() / ".mem0" / "qdrant",
            Path.home() / ".mem0",
        ]
        for p in candidates:
            if p.exists():
                return p
        return None

    def list_all(self, limit: int = 50) -> list[dict]:
        """列出 Mem0 的记忆。V1.0 读取本地 Qdrant 的 collections。"""
        mem0_dir = self._get_mem0_dir()
        if not mem0_dir:
            return []

        # V1.0: 简单实现 — 读取 Mem0 的本地持久化目录中的 collection 文件
        # 完整实现需要 qdrant-client 读取本地存储
        results = []
        try:
            # 尝试通过 qdrant-client 读取
            from qdrant_client import QdrantClient
            client = QdrantClient(path=str(mem0_dir))
            collections = client.get_collections().collections
            for col in collections:
                points = client.scroll(
                    collection_name=col.name,
                    limit=min(limit, 100),
                    with_payload=True,
                    with_vectors=False,
                )
                for point in points[0]:
                    results.append({
                        "id": str(point.id),
                        "memory": point.payload.get("text", "") or point.payload.get("data", ""),
                        "metadata": point.payload,
                        "source": "mem0",
                        "score": 1.0,
                    })
        except ImportError:
            # qdrant-client 未安装，返回空
            pass
        except Exception:
            pass

        return results[:limit]

    def get(self, memory_id: str) -> dict | None:
        """获取单条 Mem0 记忆。"""
        try:
            from qdrant_client import QdrantClient
            mem0_dir = self._get_mem0_dir()
            if not mem0_dir:
                return None
            client = QdrantClient(path=str(mem0_dir))
            # 遍历所有 collection 查找
            collections = client.get_collections().collections
            for col in collections:
                points = client.scroll(
                    collection_name=col.name,
                    limit=1,
                    with_payload=True,
                    with_vectors=False,
                )
                for point in points[0]:
                    if str(point.id) == memory_id:
                        return {
                            "id": str(point.id),
                            "memory": point.payload.get("text", ""),
                            "metadata": point.payload,
                            "source": "mem0",
                        }
        except Exception:
            pass
        return None
```

- [ ] **Step 2: Basic Memory (Markdown) 适配器**

```python
"""Basic Memory 适配器 — 读取 Markdown 记忆文件。"""

from pathlib import Path
from datetime import datetime
from agent_memory_mcp.backends.adapters.base import MemoryAdapter


class BasicMemoryAdapter(MemoryAdapter):
    """读取 Basic Memory 的 Markdown 文件。"""

    @property
    def name(self) -> str:
        return "basic_memory"

    def _get_basic_memory_dir(self) -> Path | None:
        """检测 Basic Memory 数据目录。"""
        candidates = [
            Path.home() / ".basic-memory",
            Path.home() / "basic-memory",
        ]
        for p in candidates:
            if p.exists() and list(p.glob("*.md")):
                return p
        return None

    def list_all(self, limit: int = 50) -> list[dict]:
        """列出 Basic Memory 的记忆 — 读取 Markdown 文件中的记忆块。"""
        base_dir = self._get_basic_memory_dir()
        if not base_dir:
            return []

        results = []
        for md_file in sorted(base_dir.glob("*.md"), reverse=True)[:50]:
            content = md_file.read_text(encoding="utf-8")
            # 按 --- 分割成记忆块
            blocks = content.split("\n---\n") if "---" in content else [content]
            for block in blocks:
                block = block.strip()
                if not block:
                    continue
                results.append({
                    "id": f"bm_{md_file.stem}_{hash(block) % 10000000:07d}",
                    "memory": block[:500],
                    "metadata": {
                        "source_file": str(md_file),
                        "created_at": datetime.fromtimestamp(md_file.stat().st_mtime).isoformat(),
                    },
                    "source": "basic_memory",
                    "score": 1.0,
                })
                if len(results) >= limit:
                    break
            if len(results) >= limit:
                break

        return results[:limit]

    def get(self, memory_id: str) -> dict | None:
        """获取单条 Basic Memory 记忆。"""
        # V1.0 简化实现 — 搜索文件名中的 ID
        base_dir = self._get_basic_memory_dir()
        if not base_dir:
            return None
        # 从 ID 提取文件名
        parts = memory_id.split("_", 2)
        if len(parts) >= 2:
            md_file = base_dir / f"{parts[1]}.md"
            if md_file.exists():
                content = md_file.read_text(encoding="utf-8")
                return {
                    "id": memory_id,
                    "memory": content[:500],
                    "metadata": {"source_file": str(md_file)},
                    "source": "basic_memory",
                }
        return None
```

- [ ] **Step 3: 适配器注册**

在 `mcp-server/src/agent_memory_mcp/__init__.py` 或 `server.py` 启动时注册适配器。

在 `server.py` 添加：

```python
from agent_memory_mcp.core import register_adapter

# 启动时注册适配器
try:
    from agent_memory_mcp.backends.adapters.mem0_adapter import Mem0Adapter
    register_adapter(Mem0Adapter())
    logger.info("registered Mem0 adapter")
except Exception:
    pass

try:
    from agent_memory_mcp.backends.adapters.md_adapter import BasicMemoryAdapter
    register_adapter(BasicMemoryAdapter())
    logger.info("registered Basic Memory adapter")
except Exception:
    pass
```

- [ ] **Step 4: 写适配器测试**

```python
"""适配器测试"""

import sys
from pathlib import Path
sys.path.insert(0, str(Path(__file__).resolve().parent.parent / "src"))

from agent_memory_mcp.backends.adapters.base import MemoryAdapter


class TestAdapterInterface:
    def test_base_class_cannot_be_instantiated(self):
        try:
            MemoryAdapter()  # type: ignore
            assert False, "should have raised TypeError"
        except TypeError:
            pass

    def test_concrete_adapter_works(self):
        class Impl(MemoryAdapter):
            @property
            def name(self): return "test"
            def list_all(self, limit=50): return [{"id": "1"}]
            def get(self, id): return {"id": id}
        impl = Impl()
        assert impl.name == "test"
        assert impl.list_all() == [{"id": "1"}]
        assert impl.get("1") == {"id": "1"}
```

- [ ] **Step 5: 导入/导出 API**

`memories.py` 添加导出端点：

```python
@router.get("/memories/export")
def export_memories(project_id: str | None = None):
    """导出全部记忆为 JSON。"""
    mems = mem0_backend.list_all(project_id=project_id, limit=100000)
    return {
        "exported_at": datetime.now(timezone.utc).isoformat(),
        "version": "1.0",
        "count": len(mems),
        "memories": mems,
    }
```

- [ ] **Step 6: 运行测试**

`pytest packages/mcp-server/tests/test_adapters.py -v`
Expected: 全部通过

- [ ] **Step 7: Commit**

```bash
git add packages/mcp-server/src/agent_memory_mcp/backends/adapters/
git add packages/mcp-server/src/agent_memory_mcp/server.py
git add packages/mcp-server/tests/test_adapters.py
git add packages/dashboard/backend/src/routers/memories.py
git commit -m "feat: Mem0 + Basic Memory adapters, export API"
```

---

### Task 6: 联调 + Bug Bash（Day 6）

**Files:** 不需要新增，全流程测试

- [ ] **Step 1: 全流程测试**

```bash
# 1. 启动 MCP 服务器（终端 1）
cd packages/mcp-server
python -m agent_memory_mcp.server

# 2. 启动 Dashboard（终端 2）
cd packages/dashboard/backend
python src/main.py
```

手动走通：
- Dashboard 打开 → 显示记忆列表
- 点击编辑 → 弹出编辑框 → 保存 → 列表更新
- 点击删除 → 确认 → 删除成功
- 编辑次数显示正确（100 次基础）
- CLI `agent-memory refer` → 输出邀请码

- [ ] **Step 2: 运行所有测试**

`pytest packages/mcp-server/tests/ -v`
Expected: 全部 86+ 测试通过

`cd packages/cli && npm test`
Expected: 全部通过

- [ ] **Step 3: 修复联调发现的问题**

（根据测试结果修改）

- [ ] **Step 4: Commit**

```bash
git add -A
git commit -m "fix: integration fixes from V1.0 full flow test"
```

---

### Task 7: 发布准备 + 文档（Day 7）

**Files:**
- Modify: `packages/mcp-server/pyproject.toml` (版本号)
- Modify: `packages/cli/package.json` (版本号)
- Modify: `README.md`

- [ ] **Step 1: 更新 README.md**

新增内容：
- 记忆管理功能介绍
- 编辑/删除操作说明
- 裂变推荐说明
- Dashboard 截图指引

- [ ] **Step 2: 更新版本号**

`packages/mcp-server/pyproject.toml` → version = "1.0.0"
`packages/cli/package.json` → version → "1.0.0"

- [ ] **Step 3: 生成 CHANGELOG**

基于 git log 生成 v0.5 → v1.0 的变更日志。

- [ ] **Step 4: 最终测试**

`pytest packages/mcp-server/tests/ -v` → 全部通过
`cd packages/cli && npm test` → 全部通过

- [ ] **Step 5: Commit**

```bash
git add README.md packages/mcp-server/pyproject.toml packages/cli/package.json
git commit -m "chore: bump to v1.0, update docs"
```

---

## PRD 覆盖检查

| PRD 功能 | 对应 Task | 状态 |
|----------|----------|------|
| F1 记忆浏览器 | Task 2 | ✅ 已有 |
| F2 记忆编辑 | Task 2 | ✅ |
| F3 记忆删除 | Task 2 | ✅ |
| F4 编辑限制 100次/月+裂变 | Task 3 | ✅ |
| F5 裂变推荐 | Task 3 | ✅ |
| F6 升级引导 | Task 3 | ✅ |
| F7 同步层 | Task 4 | ✅ |
| F8 Mem0 适配器 | Task 5 | ✅ |
| F9 Basic Memory 适配器 | Task 5 | ✅ |
| F10 首次引导 | Task 2 (空状态) | ✅ |
| F11 导入/导出 | Task 5 | ✅ |
| Bug 修复 | Task 1 | ✅ |
| 去重 _append_session_log | Task 1 | ✅ |
