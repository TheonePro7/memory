{
  "name": "agent-memory-monorepo",
  "private": true,
  "description": "Agent Memory — 让 AI Agent 拥有记忆能力的元认知层产品",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces"
  }
}
